# dongfeng-pay 测试指南

> 系统测试、数据验证和性能压测完整指南

---

## 📋 目录

1. [系统测试套件](#系统测试套件)
2. [数据完整性验证](#数据完整性验证)
3. [性能压测](#性能压测)
4. [性能监控](#性能监控)
5. [测试清单](#测试清单)

---

## 🧪 系统测试套件

### 测试文件位置

```
tests/system_test.go
```

### 运行所有测试

```bash
cd tests
go test -v
```

### 运行特定测试

```bash
# 健康检查测试
go test -v -run TestBossHealth
go test -v -run TestMerchantHealth
go test -v -run TestAgentHealth
go test -v -run TestGatewayHealth

# 功能测试
go test -v -run TestBossLogin
go test -v -run TestGatewayPaymentAPI
go test -v -run TestGatewayRefundAPI

# 性能测试
go test -v -run TestResponseTime
go test -v -run TestConcurrentRequests
go test -v -run TestMemoryLeak
```

### 测试覆盖范围

#### 1. 健康检查测试
- ✅ Boss 服务可用性
- ✅ Merchant 服务可用性
- ✅ Agent 服务可用性
- ✅ Gateway 服务可用性

#### 2. 功能测试
- ✅ Boss 登录功能
- ✅ Gateway 支付接口
- ✅ Gateway 退款接口（新增）

#### 3. 性能测试
- ✅ 响应时间测试
- ✅ 并发请求处理
- ✅ 内存泄漏检测

#### 4. 数据库测试
- ✅ 数据库连接测试

### 测试结果示例

```
=== RUN   TestAll
=== RUN   TestAll/健康检查
=== RUN   TestAll/健康检查/Boss
✓ Boss 服务健康检查通过
=== RUN   TestAll/健康检查/Gateway
✓ Gateway 服务健康检查通过
...
=== RUN   TestAll/性能测试
Boss 服务响应时间: 45ms
Gateway 服务响应时间: 23ms
✓ 并发请求处理正常
```

---

## 🔍 数据完整性验证

### 工具位置

```
tools/data_integrity_check.go
```

### 编译工具

```bash
cd tools
go build -o data-check data_integrity_check.go
```

### 运行检查

```bash
# 使用默认配置
./data-check

# 使用环境变量指定数据库
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASSWORD=your_password
export DB_NAME=juhe_pay

./data-check
```

### 检查项目

#### 1. 数据表完整性
- ✅ 检查所有必需表是否存在
- ✅ 统计各表数据量
- ✅ 检测缺失的表

#### 2. 订单数据完整性
- ✅ 订单状态分布
- ✅ 订单金额合理性（最小/最大/平均）
- ✅ 孤立订单检测（商户不存在）

#### 3. 账户余额完整性
- ✅ 负余额账户检测
- ✅ 系统总余额统计

#### 4. 退款数据完整性
- ✅ 退款状态分布
- ✅ 退款金额统计

### 输出示例

```
==================================================================
   数据表完整性检查
==================================================================
✓ user_info                   存在, 行数: 5
✓ merchant_info                存在, 行数: 120
✓ order_info                   存在, 行数: 5432
✓ refund_info                  存在, 行数: 89
==================================================================
总计: 20/20 个表存在, 总行数: 12847
==================================================================

==================================================================
   订单数据完整性检查
==================================================================
订单状态分布:
  success              : 4521
  fail                 : 678
  pending              : 233

订单金额统计:
  最小金额: 1.00
  最大金额: 9999.00
  平均金额: 235.67

✓ 未发现孤立订单
==================================================================
```

---

## ⚡ 性能压测

### 压测工具位置

```
tools/benchmark.go
```

### 编译工具

```bash
cd tools
go build -o benchmark benchmark.go
```

### 使用方法

#### 1. 预定义场景

```bash
# Boss 服务压测（50并发，10秒）
./benchmark -scenario boss

# Gateway 压力测试（100并发，30秒）
./benchmark -scenario gateway

# 支付接口压测（50并发，10秒）
./benchmark -scenario payment
```

#### 2. 自定义压测

```bash
# 基本用法
./benchmark -url <URL> -c <并发数> -d <持续时间>

# 示例：压测登录页面
./benchmark -url http://localhost:12306/login.html -c 100 -d 30

# POST 请求
./benchmark -url http://localhost:12309/gateway/scan \
  -method POST \
  -c 50 \
  -d 10 \
  -body '{"merchant_id":"test","amount":100}'
```

#### 3. 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `-url` | 测试URL | 必需 |
| `-method` | HTTP方法（GET/POST） | GET |
| `-c` | 并发数 | 50 |
| `-d` | 持续时间（秒） | 10 |
| `-t` | 请求超时（秒） | 5 |
| `-body` | 请求体（POST） | {} |
| `-scenario` | 预定义场景 | - |

### 压测结果示例

```
======================================================================
   dongfeng-pay 性能压测工具
======================================================================
目标URL: http://localhost:12309/
并发数: 100
持续时间: 30s
======================================================================

开始压测...

======================================================================
   压测结果
======================================================================
总请求数:     152,437
成功请求数:   151,892 (99.64%)
失败请求数:   545 (0.36%)

总耗时:       30.02s
QPS:          5077.23 请求/秒

延迟统计:
  最小:       2.3ms
  最大:       1.2s
  平均:       18.5ms

百分位延迟:
  P50:        15.2ms
  P90:        32.5ms
  P95:        48.3ms
  P99:        156.8ms
======================================================================
```

### 性能基准

| 指标 | 良好 | 一般 | 需优化 |
|------|------|------|--------|
| **QPS** | > 5000 | 2000-5000 | < 2000 |
| **平均延迟** | < 20ms | 20-50ms | > 50ms |
| **P99延迟** | < 100ms | 100-500ms | > 500ms |
| **成功率** | > 99.9% | 99%-99.9% | < 99% |

---

## 📊 性能监控

### 监控中间件

```
gateway/middleware/monitor.go
```

### 启用监控

在 `gateway/routers/router.go` 中添加：

```go
import "gateway/middleware"

func init() {
    // 添加性能监控过滤器
    web.InsertFilter("/*", web.BeforeRouter, middleware.PerformanceMonitorFilter)

    // 添加健康检查过滤器
    web.InsertFilter("/*", web.BeforeRouter, middleware.HealthCheckFilter)

    // ... 其他路由
}
```

### 查看监控数据

#### 1. 健康检查接口

```bash
# 查看系统健康状态和性能指标
curl http://localhost:12309/health

# 或
curl http://localhost:12309/ping
```

#### 2. 响应示例

```json
{
  "status": "ok",
  "metrics": {
    "总请求数": 12847,
    "成功请求数": 12698,
    "失败请求数": 149,
    "当前请求数": 5,
    "平均延迟": "23.5ms",
    "最小延迟": "1.2ms",
    "最大延迟": "1.2s",
    "运行时间": "2h15m32s",
    "QPS": "1.58",
    "成功率": "98.84%",
    "内存使用": "45.23 MB",
    "总内存": "128.67 MB",
    "协程数": 23,
    "路由统计": {
      "/gateway/scan": 8547,
      "/gateway/refund/create": 892,
      "/gateway/balance": 3408
    }
  }
}
```

### 监控指标说明

| 指标 | 说明 |
|------|------|
| **总请求数** | 启动以来的总请求数 |
| **成功请求数** | 2xx, 3xx 状态的请求数 |
| **失败请求数** | 4xx, 5xx 状态的请求数 |
| **当前请求数** | 正在处理的请求数 |
| **平均延迟** | 所有请求的平均响应时间 |
| **最小/最大延迟** | 最快/最慢的响应时间 |
| **QPS** | 每秒请求数 |
| **成功率** | 成功请求的百分比 |
| **内存使用** | 当前内存占用 |
| **协程数** | 当前活跃的 goroutine 数量 |
| **路由统计** | 各路由的请求分布 |

### 自动性能报告

监控中间件会每 5 分钟自动输出性能报告到日志：

```
=== 性能指标报告 ===
总请求数: 12847
成功请求数: 12698
失败请求数: 149
平均延迟: 23.5ms
QPS: 1.58
成功率: 98.84%
路由统计:
  /gateway/scan: 8547
  /gateway/refund/create: 892
  /gateway/balance: 3408
==================
```

---

## ✅ 测试清单

### 升级后必须测试的项目

#### 1. 服务可用性 ✅
- [ ] Boss 服务启动成功
- [ ] Merchant 服务启动成功
- [ ] Agent 服务启动成功
- [ ] Gateway 服务启动成功

#### 2. 数据库连接 ✅
- [ ] PostgreSQL 18 连接成功
- [ ] 所有表存在
- [ ] 数据迁移完整

#### 3. 核心功能 ✅
- [ ] 登录功能正常（Boss/Merchant/Agent）
- [ ] 支付接口可访问
- [ ] 退款接口可访问（新增）
- [ ] 订单查询正常
- [ ] 代付功能正常

#### 4. 性能指标 ✅
- [ ] 响应时间 < 50ms
- [ ] 并发处理能力 > 100 req/s
- [ ] 无明显内存泄漏
- [ ] 数据库查询性能提升

#### 5. 数据完整性 ✅
- [ ] 无孤立订单
- [ ] 无负余额账户（除非业务允许）
- [ ] 退款数据一致性

#### 6. 监控和日志 ✅
- [ ] 性能监控数据正常
- [ ] 日志输出正常
- [ ] 慢请求告警正常

---

## 🚀 快速测试脚本

创建 `quick_test.sh` 用于快速测试：

```bash
#!/bin/bash

echo "========================================="
echo "   dongfeng-pay 快速测试"
echo "========================================="

# 1. 系统测试
echo "\n1. 运行系统测试..."
cd tests && go test -v -run TestAll

# 2. 数据完整性检查
echo "\n2. 数据完整性检查..."
cd ../tools && ./data-check

# 3. 性能压测
echo "\n3. 性能压测（Boss服务）..."
./benchmark -scenario boss

echo "\n========================================="
echo "   测试完成"
echo "========================================="
```

使用：

```bash
chmod +x quick_test.sh
./quick_test.sh
```

---

## 📝 注意事项

1. **测试环境隔离**
   - 不要在生产环境运行压测
   - 使用独立的测试数据库

2. **数据备份**
   - 测试前备份数据库
   - 保存测试结果日志

3. **资源监控**
   - 监控 CPU 和内存使用
   - 注意数据库连接数

4. **逐步压测**
   - 从小并发开始
   - 逐步增加压力
   - 观察系统极限

5. **定期测试**
   - 每次代码变更后测试
   - 定期进行性能基准测试
   - 记录性能趋势

---

**文档版本**: v1.0
**更新时间**: 2025-11-17
**维护者**: Claude AI

🎯 **祝测试顺利！**
